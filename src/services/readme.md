## services/ – API-Zugriffe / Datenlogik

Inhalt: Abstraktion von Backend-Requests, z. B. authService.ts, userService.ts.

Funktion:
Kommuniziert mit dem Backend (REST, GraphQL), kapselt Axios- oder Fetch-Calls.
import styled from "styled-components";

export const HomeContainer = styled.div`
  /* styles will be added later, keep className for now */
`;

export const HomeMain = styled.main`
  /* styles will be added later, keep className for now */
`;
